//import chat.out for output
import("chat.out");

//setup variable
int binary = 1;

//stop when binary < 0 as 1073741824 * 2 is -2147483648 for minecraft
while(binary > 0)
{
	//output variable
	out(binary);

	//multiplicate with 2
	binary *= 2;
}
