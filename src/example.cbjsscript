a = 1;
a += 3;
a *= 3;

b = 3;
b *= a;

outputAll();

while(a < 42)
{
    a++;
    if(a == b)
        tellraw("if a isnt 36 now smomething is wrong");
}

function outputAll()
{
    tellraw("Value of a: ", a, "\nValue of b: ", b);
}
